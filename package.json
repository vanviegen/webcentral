{
  "name": "node-central",
  "version": "0.1.1",
  "description": "Host a directory of Node apps each on a subdomain, running them on-demand in separate sandboxes.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "node ./conf/quickfix.js"
  },
  "author": "Frank van Viegen",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "express-http-proxy": "^1.2.0",
    "get-port": "^4.0.0",
    "greenlock-express": "^2.4.6"
  }
}
